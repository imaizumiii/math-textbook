# math_generator/config.yaml

title: "一次方程式 計算ドリル"
num_problems: 25        # 生成する問題数
variable_name: "x"      # 変数の名前
# 解 x の範囲
solution_range:
  min: -5
  max: 5
# 係数 a の範囲
coefficient_a_range:
  min: 2
  max: 10
# 定数項 b の範囲 (c は a, b, x から計算される)
coefficient_b_range:
  min: -10
  max: 10